#!/usr/bin/env python3
"""Fixture stub: checks a changelog entry against the house style rules.

Not actually invoked by any test — this file exists so
`skill ingest` has a real script to map into a stub capability.
"""
import sys

def main() -> int:
    print("ok: fixture lint script, not a real implementation")
    return 0

if __name__ == "__main__":
    sys.exit(main())
